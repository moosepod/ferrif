/// This file is auto-generated by running `cargo about generate about.hbs > src/app/licenses.rs`. It needs to be
/// re-saved as UTF8
use eframe::egui;
use egui::*;

pub fn oss_licenses(ui: &mut eframe::egui::Ui) {
{{#each licenses}}
ui.add(egui::Label::new(RichText::new("{{name}}".replace("&quot;","\"")).heading()));
ui.label("Used by:\n");
{{#each used_by}}
ui.hyperlink_to("{{crate.name}} ({{crate.version}})","{{#if crate.repository}} {{crate.repository}} {{else}}
https://crates.io/crates/{{crate.name}} {{/if}}");
{{/each}}
ui.label("{{text}}".replace("&quot;","\"").as_str().replace("&#x27;","'"));
ui.separator();
{{/each}}
}